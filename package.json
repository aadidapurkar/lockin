{
    "name": "lockin",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "aiscript": "rm -rf dist && mkdir -p dist && cp -R public/* dist/ && npx esbuild src/service_worker.ts --bundle --platform=browser --outfile=dist/service_worker.js && npx esbuild src/main.ts --bundle --platform=browser --outfile=dist/main.js",
        "ai2": "rm -rf dist && mkdir -p dist && cp -R public/* dist/ && cp src/style.css dist/ && cp src/types.ts dist/ && npx esbuild src/service_worker.ts --bundle --platform=browser --outfile=dist/service_worker.js && npx esbuild src/main.ts --bundle --platform=browser --outfile=dist/main.js"
    },
    "devDependencies": {
        "@types/chrome": "^0.1.31",
        "esbuild": "^0.27.0",
        "typescript": "~5.9.3",
        "vite": "^7.2.2"
    },
    "dependencies": {
        "rxjs": "^7.8.2"
    }
}
